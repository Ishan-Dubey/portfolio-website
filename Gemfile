source "https://rubygems.org"

gem "github-pages", group: :jekyll_plugins  # includes Jekyll core + supported plugins
gem "jekyll-remote-theme"                  # enables `remote_theme:` in _config.yml

